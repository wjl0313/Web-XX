# P0 冻结功能清单

P0 冻结期间，以下功能保留旧存档字段和 legacy 查看能力，但不得进入默认原生主流程，也不得在后台自行推进。

当前验收状态：已完成。默认原生导航没有这些入口，对应网络连接、定时器和结算均未启动；旧数据可在设置页“旧版兼容工具”读取。

## 1. 默认原生入口完全隐藏

| 功能 | P0 行为 | 旧数据处理 |
| --- | --- | --- |
| 赌石坊及赌场玩法 | 不显示入口，不执行统计或奖励 | 保留全部赌场字段 |
| Discord 社群 | 不显示入口，不请求人数 | 保留旧设置 |
| 管理员工具 | 不打包到玩家导航 | 保留 legacy 管理入口 |
| 聊天 | 不连接实时订阅 | 保留频道和屏蔽字段 |
| 宗门 | 不拉取、不结算、不推进周目标 | 保留宗门快照字段 |
| LFG 借用英雄 | 不发布、不借用、不领取 | 保留队伍与奖励字段 |

## 2. 只读兼容

| 功能 | P0 允许 | P0 禁止 |
| --- | --- | --- |
| 悟道 | 读取点数、节点和预设摘要 | 投点、洗点、模拟、自动分流 |
| 转世重修/飞升 | 读取层数、点数和永久收益 | 飞升、投入、重置 |
| 符纹/禁制套装 | 读取仓库、镶嵌和配方结果 | 合成、镶嵌、拆毁、演化 |
| 护道者/同行 | 读取成员、阵型、气血和装备 | 招募、解雇、工资结算、改阵型 |
| 灵兽 | 读取已拥有与当前激活项 | 驯服、放生、切换 |
| 旧炼丹 | 读取等级、材料、配方和库存 | 炼制、购买、出售、自动炼丹 |
| 神龛 | 读取当前效果 | 祈愿和扣费 |
| 套装收藏 | 读取发现状态和保底进度 | 领取、指定目标、推进保底 |

## 3. 实现约束

- 冻结功能必须由显式功能开关控制。
- 原生入口不得注册对应定时器、轮询或实时订阅。
- 加载和保存必须保留未知字段，不能因为不显示而删除。
- legacy 模式继续作为完整查看器。
- P0 不新增区域、妖物、装备、功法、任务、首领、丹方、符纹配方、悟道节点、重修天赋或赌场奖励。
