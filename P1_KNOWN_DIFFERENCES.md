# P1 已知差异报告

本文记录 v2 与 legacy 的有意差异，以及 P1 结束时仍未进入范围的事项。

## 1. 有意规则差异

- legacy 继续使用旧即时命令顺序、旧功法快捷栏、旧挂机和旧装备效果；v2 使用每轮身法先攻的 1v1 状态机。
- v2 固定三个出战功法位；普通攻击、调息和遁走不占槽。
- v2 只读取五行、抗性、灵根亲和和 P1 装备白名单；悟道、转世、符纹、套装、宠物、队伍等旧加成不进入公式。
- v2 失败不会触发生死劫永久删档。
- v2 奖励继续使用冻结经验曲线和基础升级成长，因为 P1 明确禁止调整经验曲线与正式境界突破。

## 2. 内容范围差异

- v2 开放 4 传承、31 灵根、全部 41 区域、全部旧版普通/命名/首领怪物、12 功法和全部旧版装备；早期 5 区域/22 怪物/29 装备的白名单已按产品要求移除。
- 其余旧内容没有删除；在 v2 中标记为未开放或不生效，在 legacy 中仍可使用。
- P1 没有新增功法获取经济；每个传承初始获得三门功法，存档结构允许后续阶段保留其他已获得的 P1 功法。

## 3. 呈现差异

- P1 沿用 P0 恢复后的旧式场景、角色/妖兽画面、三栏、标签页、六槽纸娃娃和装备比较，不做新皮肤。
- v2 在旧位置增加回合、行动者、五行、抗性、克制、三功法状态和最近伤害分解文字。
- v2 右侧挂机栏改为“单场自动战斗/冻结范围”说明；legacy 角色仍显示完整旧挂机栏。

## 4. 非阻断限制

- 活跃中的 v2 `BattleState` 当前保存在运行时 Store；整页刷新会放弃尚未结算的单场战斗，但角色气血、法力和已完成奖励会正常持久化，不会损坏存档。
- P1 内容中的眩晕/冻结状态机能力已实现并有测试，但首批 12 门功法没有加入眩晕或冻结功法。
- 战斗画面继续使用现有场景和形象表现；新战斗动画、最终视觉资源和统一皮肤等待产品后续单独设计。
- CloudBase 仍是隔离准备层，没有部署或联调。

## 5. 回归缺陷判定

以下情况不属于已知差异，出现即视为缺陷：

- legacy 角色进入 v2 公式，或 v2 角色调用旧战斗/挂机公式。
- 一轮任一单位行动超过一次，或身法没有在下一轮重新计算。
- 功法超过三个、战斗中可换功法、法力/冷却/目标校验被绕过。
- 五行、抗性、亲和或伤害分解不可见或计算顺序改变。
- 六槽装备、装备对比、战斗场景、角色/妖兽画面或旧 UI 骨架被替换。
- 冻结 HTML 哈希变化、旧存档字段丢失、刷新卡在 Loading 或页面交互失效。
