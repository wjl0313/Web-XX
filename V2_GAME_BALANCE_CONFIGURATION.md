# 凡修录 v2 全局数值配置与战后恢复建议

## 1. 配置入口与维护边界

v2 玩法的统一数值入口为：

- 代码配置：`src/game-core/rulesets/v2/balance.config.ts`
- 运行时配置：设置 -> 数值配置台
- 本机持久化键：`fanxiulu:v2-game-balance:v1`
- 完整交换格式：配置台 JSON 页签导入、复制或下载

配置独立于角色存档。创建新角色、普通战斗、自动历练、离线模拟、秘境、突破、装备、丹药和调息读取同一个运行时配置。战斗、自动历练或调息期间禁止应用新配置，避免一次行动在两套参数之间结算。

当前集中管理的参数：

| 分组 | 内容 | 生效时机 |
| --- | --- | --- |
| 职业 | 四职业初始资源、攻击、防御、六维 | 新建角色 |
| 成长 | 五条突破路线单次成长、四职业逐属性成长倍率 | 后续突破 |
| 功法 | 12 门功法的消耗、冷却、基础威力、攻击/神识/体魄/六维缩放、持续时间、效果强度 | 下一次行动 |
| 五行 | 金木水火土克制表、克制/被克/同属性/无关系倍率 | 下一次伤害 |
| 战斗 | 致命概率和倍率、命中、闪避、防御曲线、伤害浮动、遁走概率 | 下一场战斗 |
| 装备 | 全部旧版装备（69 件）的资源、攻击、防御、六维、抗性和固定伤害倍率 | 新装备；已穿装备在应用配置时按差值重算 |
| 怪物 | 全部旧版怪物（普通 123、精英 82、首领 19）的等级、资源、战斗属性、抗性、修为和灵石 | 下一场战斗 |
| 逐怪掉落 | 绑定装备和概率、随机装备池、灵草池、功法池、各类掉率和材料数量 | 下一次奖励 |
| 恢复 | 战胜恢复、自然调息、回春丹、回灵丹 | 下一次对应结算 |

内容开放：v2 已从早期 5 个区域、22 只怪物和 29 件装备的 P1 白名单，扩展为旧版全部 41 个历练区域、全部普通/命名/首领怪物和全部装备。自动历练、手动斗法、离线模拟和秘境统一读取同一套全量目录。

## 2. 当前成长和资源池审计

按现有四职业、五条成长路线和开放装备估算：

- 初始最大气血约 80～160，初始最大法力约 40～160。
- 当前公开成长闭环约 18 次突破；常规中后期资源池约 300～500。
- 极端炼体或神识路线叠加现有装备后，最大气血或法力约 500～600。
- 境界表已预留 40～60 级以上空间；若继续扩展，单项属性与修为等级会同时增长。
- 装备会直接修改最大资源和六维，恢复公式读取角色结算后的 `maxHp`、`maxMp`、体魄 `str` 和神识 `wis`，因此装备贡献无需另设重复倍率。

直接采用 `10% 最大资源 + 属性 × 修为等级` 时，属性等级项会二次增长。约 20～40 级后，该项很容易接近或超过整个资源池，导致每场胜利回满，调息、丹药和治疗功法失去价值。

## 3. 推荐战胜恢复公式

气血与法力使用同一曲线，但分别读取体魄和神识：

```text
基础恢复 = 最大资源 × 基础恢复比例
原始成长项 = 对应属性 × 修为等级 × 属性等级系数
成长软上限 = 最大资源 × 成长软上限比例
有效成长项 = 原始成长项 / (1 + 原始成长项 / 成长软上限)
理论恢复 = floor(基础恢复 + 有效成长项)
最终恢复 = min(缺失资源, 理论恢复, 最大资源 × 单次硬上限比例)
```

推荐默认值：

| 参数 | 气血 | 法力 |
| --- | ---: | ---: |
| 基础恢复比例 | 10% | 10% |
| 属性等级系数 | 0.10 | 0.10 |
| 成长软上限比例 | 25% | 25% |
| 单次硬上限比例 | 35% | 35% |

该设计保留了“10% + 属性 × 等级”结构，但用系数控制量级，用软上限消除后期二次增长，用 35% 硬上限防止异常装备或未来境界直接回满。

示例估算：

| 阶段 | 最大气血 / 体魄 / 等级 | 气血恢复 | 最大法力 / 神识 / 等级 | 法力恢复 |
| --- | --- | ---: | --- | ---: |
| 初期 | 160 / 15 / 1 | 17（10.6%） | 40 / 10 / 1 | 4（10.0%） |
| 前中期 | 250 / 22 / 10 | 41（16.4%） | 200 / 22 / 10 | 35（17.5%） |
| 中期 | 400 / 32 / 20 | 79（19.8%） | 350 / 35 / 20 | 73（20.9%） |
| 后期 | 600 / 45 / 40 | 141（23.5%） | 500 / 50 / 40 | 126（25.2%） |
| 扩展期 | 900 / 60 / 60 | 228（25.3%） | 750 / 70 / 60 | 204（27.2%） |

结算顺序固定为：

```text
战斗结果 -> 胜利奖励 -> 战胜恢复 -> 以恢复后的气血判断调息阈值 -> 保存
```

只有胜利触发战胜恢复。战败仍保留 1 点气血并进入调息判断，避免战败等同免费回满；逃离不触发胜利恢复。

## 4. 战斗默认值

为保持当前体验，命中和闪避配置的推荐默认值是：基础命中率 100%，基础闪避率 0%，身法命中和闪避系数均为 0，最低命中率 5%，最高命中率 100%。因此默认行为与修改前一致，不会额外产生未命中。

致命一击默认值为：基础概率 5%，每点身法增加 0.2%，概率上限 50%，致命一击倍率 1.5。防御减伤仍使用 `常数 / (常数 + 防御)`，默认常数 100，最低承伤倍率 25%。这些值已进入配置台，不再是战斗公式中的匿名常数。

## 4.1 场景灵力充沛度与被动法力恢复

每个历练区域都有独立的灵力充沛度，默认按区域等级从约 1.0 递增到约 3.4，并可在设置 -> 数值配置台 -> 场景灵力中逐区域调整。当前区域充沛度会显示在角色状态栏和历练区域页。

法力不依赖调息恢复；调息只恢复气血。法力按时间被动恢复，公式为：

```text
每秒法力恢复 = max(保底值, 基础值 + 神识 × 灵力充沛度 × 神识系数) × (1 + 境界序号 × 境界系数)
```

默认参数：

| 参数 | 默认值 | 说明 |
| --- | ---: | --- |
| 法力基础恢复/秒 | 0.5 | 属性为 0 时也保留的基础恢复 |
| 每点神识恢复系数 | 0.15 | 神识越高，场景恢复越快 |
| 境界成长系数 | 0.08 | 每提升一档境界，恢复按此比例成长 |
| 法力恢复每秒保底 | 0.25 | 低属性角色也不会完全无恢复 |

神识读取六维 `wis`，境界序号按炼气一层起计算。被动恢复按真实时间累计，在线定时器、调息期间、自动历练和离线模拟共用同一公式；回灵丹和回灵功法仍作为主动加速手段保留。

## 5. 逐怪掉落统一

此前手动战斗会额外加入怪物绑定装备，而自动历练和秘境只走通用随机池。现在四条路径统一调用逐怪掉落配置：

- 绑定装备 ID 与绑定装备掉率。
- 每只怪独立的随机装备掉率和装备池。
- 每只怪独立的灵草掉率、抽取次数、数量倍率和灵草池。
- 每只怪独立的功法掉率和功法池，最终仍受灵根可学习范围限制。
- 妖丹、首领精魄和首通精魄数量。

普通战斗、自动历练、离线精确模拟和秘境因此不会再出现同一只怪在不同入口使用不同掉落规则的问题。

## 6. 调参建议

1. 先复制或下载当前 JSON，作为实验基线。
2. 每次只调整一个系统分组，并用固定角色、固定区域和固定战斗 seed 对比。
3. 战后恢复优先调整属性等级系数；只有整体恢复偏高或偏低时再改 10% 基础项。
4. 命中与闪避启用后，应同时观察回合数、丹药消耗和自动历练每小时收益，不能只看单次伤害。
5. 怪物属性与掉率需要成对调整；延长战斗时间而不提高收益会直接降低同区域修为效率。
6. 配置稳定后将导出的 JSON 纳入版本记录，并在重构日志注明配置版本和关键变化。
