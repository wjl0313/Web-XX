# 凡修录重构日志

本日志记录从单文件版本迁移到 Vue、TypeScript、Pinia 和框架无关 `game-core` 的每个阶段。除非阶段记录明确说明，重构期间不改变玩法数值、存档字段、装备/功法槽规则或云端协议。

冻结回归基线：

- 文件：`legacy/fanxiulu-monolith.html`
- 行数：39,468
- SHA-256：`113F9ED589375E746C50A13EC6FE29B32BC6F255B208B7FACE43D7218A2685CC`
- 游戏版本：`1.6.19`
- 存档 schema：`2`

## 阶段 0：冻结单文件基线

状态：已完成

结果：

- 将当前可运行的 39,468 行游戏保存为冻结兼容基线。
- 建立 SHA-256、入口行数和生产构建产物校验。
- 明确重构期间不得直接修改冻结文件。

验证：`scripts/verify-baseline.mjs` 同时检查源文件和构建副本哈希。

## 阶段 1：建立 Vue 项目壳

状态：已完成

结果：

- 建立 Vue 3、TypeScript、Vite 和 Pinia 项目。
- 根 `index.html` 缩减为 15 行，应用入口由 Vue 接管。
- 建立 `App.vue`、`GameView.vue`、`GameLayout.vue`、基础样式和功能开关。
- 冻结游戏通过 `LegacyGameFrame.vue` 兼容桥运行，功能未被裁剪。

兼容性：玩家仍使用完整旧版运行时；Vue 只负责加载状态和兼容容器。

## 阶段 2：建立自动化安全网

状态：已完成

结果：

- 引入 Vitest、V8 覆盖率和 Playwright。
- 增加冻结哈希、关键存档常量、桌面端/移动端登录页和本地角色道册测试。
- 增加 GitHub Actions，执行类型检查、测试、构建、基线和 E2E 验收。
- 建立只允许人工构造或脱敏数据的存档夹具目录。

验证：阶段完成时共有 9 项单元/回归测试和 4 项 E2E 测试通过。

## 阶段 3：确定性随机数边界

状态：已完成

结果：

- 在 `src/game-core/rng` 建立框架无关随机源接口和固定种子实现。
- 锁定 `test-001` 的确定性序列、整数范围、概率判断和集合选取行为。

兼容性：随机源尚未接管旧版战斗或挂机，仅作为后续算法提取的依赖注入基础。

## 阶段 4：本地存档边界

状态：已完成

结果：

- 在 `src/game-core/save` 提取 24 槽解析、补齐、截断和导入载荷识别。
- 提取旧版 FNV-1a 健康戳哈希及完整性检查。
- 建立 `SaveRepository` 和 `LocalSaveRepository`。
- 保留损坏 JSON 原文副本键规则 `EmberQuest_slots.corrupt.<timestamp>`。
- 新增人工构造的 schema 2 角色存档夹具。

兼容性：非法基础类型槽位在新边界中会记录问题并归一化为空槽；完成影子读取对比前不接管旧版读写。

验证：阶段完成时共有 29 项单元/回归测试和 4 项 E2E 测试通过，新增存档核心覆盖率 100%。

## 阶段 5：备份与便携存档格式

状态：已完成

结果：

- 提取最多 5 个本地版本备份、5 分钟自动备份间隔、内容去重和哈希校验。
- 扩展本地仓储，支持创建、列出、删除和清空备份。
- 提取纯 JSON、`EQSAVE1`、`EQSAVE2` 和 `EQSAVE3` 编解码。
- 保留 `EQSAVE2` 的来源/User-Agent 绑定和 `EQSAVE3` 的 PBKDF2-SHA256 250,000 次迭代。
- 提取 24 槽导入校验、替换/合并基础能力和角色转换注入点。
- 确认旧版本地导出没有压缩步骤，因此未创建不兼容的伪压缩格式。

兼容性：`EQSAVE1` 保留无法编码非 Latin-1 文本的旧限制；角色字段迁移仍等待静态规则模块化。

验证：阶段完成时共有 55 项单元/回归测试和 4 项 E2E 测试通过；存档核心语句覆盖率 100%、分支覆盖率 96.33%。

## 阶段 6：静态数据与中文显示表

状态：已完成

结果：

- 增加 `scripts/extract-legacy-static-data.mjs`，从冻结单体 AST 生成 TypeScript 数据模块。
- 提取 16 个传承、16 种灵根、41 个区域、69 个装备定义、21 个武器触发、9 套套装、75 条主掉落、12 个符文和 16 套职业功法表。
- 提取旧版 569 条精确中文映射，提供无 DOM 的中文显示查询。
- 增加职业、灵根、区域、装备和功法的类型守卫与目录查询 API。
- `npm run data:verify` 会检查生成文件是否仍与冻结基线一致，并已加入 `npm run verify`。

兼容性：内部英文键和数值保持不变，中文仅作为显示映射；生成文件不得手工修改。

验证：阶段完成时共有 61 项单元/回归测试通过；静态数据模块覆盖率 100%。

## 后续阶段

## 阶段 7：角色存档迁移与字段清洗

状态：已完成

结果：
- 从兼容边界抽取 `legacy-character.migration.ts`，对旧角色执行版本升级、基础类型修复、装备/背包/符文/符文之语校验、区域与尸体修复、药水迁移、功法槽初始化和公共统计字段补齐。
- 迁移保留原有 24 槽、六个装备槽、存档字段名和旧物品/怪物名称兼容规则；扩展系统通过 `migrateExtendedState` 注入，避免 `game-core` 依赖全局 DOM 状态。
- 增加角色迁移、导入清洗和字段边界测试。阶段验收时共 74 项单元/回归测试通过。

兼容性：旧存档可在不改变玩法数值的前提下进入新的核心迁移边界；赌场、公会、赛季、AA 等全局系统仍等待各自模块抽取后通过扩展钩子接入。

## 阶段 8：战斗公式与确定性战斗引擎

状态：已完成基础核心

结果：
- 从冻结单体抽取区域怪物等级、生命缩放、普通/精英/命名怪生成、玩家攻击、闪避、暴击、眩晕、护盾、怪物反击、减伤、伤害上限、经验/金币奖励、符文掉落和加权掉落层级。
- 新增无 DOM 的 `battle-engine.ts`，通过 `RandomSource` 注入随机源，支持胜利、失败、超时、事件序列、护盾消耗和暴击眩晕回合，保留旧版随机数消耗顺序。
- 新增 6 组公式测试，覆盖等级边界、生成顺序、命中/未命中、暴击/护盾、伤害上限、奖励倍率、符文和掉落分层；阶段验收共 80 项单元/回归测试通过，类型检查通过。

兼容性：基础单人战斗数值已从旧版抽离；派对、佣兵、宠物、状态持续伤害、武器触发和地下城包作为后续扩展接口，不在基础公式中隐式依赖全局运行时。

## 阶段 9：装备、掉落与功法核心

状态：已完成基础核心

结果：
- 抽取装备有效属性、物品等级缩放、防御缩放、词缀缩放、品质加成、符文镶嵌、符文之语成长、套装加成、武器触发定义和出售价值。
- 抽取品质变体生成，支持普通、魔法、稀有、史诗、传奇、神话，以及命名怪、挂机、商店、重铸、秘宝等来源差异。
- 抽取职业功法索引、等级缩放法力消耗、治疗/回蓝上限、伤害/削弱/毒伤/治疗/回蓝施放和冷却写入，均通过 `RandomSource` 保持可回放。
- 新增装备与功法测试，覆盖品质倍率、符文之语成长、套装收益、品质生成、出售价值、伤害、毒伤、治疗、回蓝、冷却、缺蓝和无目标失败路径；阶段验收共 92 项单元/回归测试通过，类型检查通过。

兼容性：装备和功法仍保留旧版英文内部键；中文显示继续由静态映射和 Vue UI 层处理。派对目标选择、佣兵支援和具体战斗日志由后续运行时层接入，不混入纯核心公式。

## 阶段 10：成长与挂机/离线结算

状态：已完成基础核心

结果：
- 抽取升级经验、职业成长、满血满蓝刷新、队伍经验分摊和 51 级后 AA 经验分流。
- 抽取普通区域挂机离线结算，保留默认 1.2 秒 tick、至少 3 tick、按 0.28 折算战斗次数和最多 1800 tick 的性能上限。
- 离线结算复用区域等级、金币衰减、掉落品质、物品出售、符文、任务、契约、声望和升级核心，不再调用旧版全局函数。
- 新增短离线忽略、任务完成、掉落/符文、升级、自动出售、长离线上限和固定种子重放测试；阶段验收共 97 项单元/回归测试通过，类型检查通过。

兼容性：本阶段覆盖普通区域挂机主路径；地下城离线、智能目标切换和队伍成员分发通过修饰参数和运行时编排接入，避免把 UI 或槽位全局变量写进模拟器。

## 阶段 11：Pinia 领域状态与运行时切换

状态：已完成

结果：

- 新增 `save`、`character`、`combat`、`afk` 四个领域 Store，并扩展 `ui` Store 管理角色道册、游戏面板和移动导航状态。
- 由 `save-repository.provider.ts` 集中提供存档仓储，组件和 Store 不再直接构造 LocalStorage 适配器。
- 接通 24 槽角色创建、选择、删除、持久化，六槽装备穿脱，区域移动，功法铭刻，药水使用，原生战斗、掉落、符文、死亡、挂机与离线恢复。
- 阶段初次验收时曾将默认入口切换为原生 Vue 运行时；阶段 13 根据 UI 等价性验收结果撤回该切换。
- 新增领域 Store 测试，使用内存仓储和固定随机源验证创建、装备、战斗胜利与离线恢复主路径。

兼容性：Pinia 只负责编排领域状态，不包含 DOM 操作或云 SDK；`game-core` 继续保持框架无关。冻结运行时仍可用于回归对照。

验证：阶段验收时类型检查通过，共 100 项单元/回归测试通过。

## 阶段 12：原生 Vue 角色、背包、战斗与挂机界面

状态：已完成

结果：

- 新增 24 槽角色道册、内联角色创建、原生游戏顶栏、功能导航和资源条。
- 新增斗法、行囊、法器、功法、云游、因果和设置面板，覆盖寻敌、普攻、施法、挂机、装备、区域移动、存档、导入导出及版本快照。
- 新增独立 `native-game.css`，建立桌面与移动布局、稳定面板尺寸、空状态、错误状态、焦点样式和减少动态效果适配；样式作用域不影响冻结 iframe。
- 将原 E2E 调整为默认原生流程与 `/?legacy=1` 兼容流程并行验证，覆盖角色创建、寻敌、面板切换和旧版本地道册。
- 使用实际浏览器检查 1280×720 和 390×844 视口；移动菜单可展开并进入行囊，两个视口均无横向溢出。

兼容性：冻结单体未修改。该界面作为迁移原型保留在 `/?native=1`，不再替换默认玩家界面；区域静态数据中尚未映射的英文说明已记入后续全中文自动审计。

验证：类型检查、100 项单元/回归测试和桌面/移动共 8 项 E2E 测试通过。

## 阶段 13：恢复旧版 UI 合同与逐屏等价门禁

状态：已完成纠偏

结果：

- 产品验收确认阶段 12 的原生界面改变了旧版信息结构、战斗表现和装备比较路径，不满足“只换皮肤、不换结构和交互”的要求。
- 默认 `/` 已恢复为冻结旧版完整界面；未达到等价门禁的 Vue 原型仅通过 `/?native=1` 进入。
- 明确后续迁移必须保留旧版三栏工作区、纸娃娃六槽、背包格、物品悬浮详情与已装备物品对比、横版战斗舞台、角色/怪物模型、标签页、弹窗和快捷操作路径。
- 允许调整的 UI 范围收窄为 CSS 设计变量、颜色、字体、纹理和不改变布局尺寸的视觉细节；结构与交互变化必须单独提出，不再混入模块化重构。
- E2E 已调整为默认入口验证冻结界面、实验入口验证 Vue 迁移原型，避免尚未等价的页面提前接管玩家流程。

兼容性：本阶段不修改冻结单体，不改变游戏数据或玩法；只调整 Vue 入口门禁和迁移验收策略。

验证：类型检查和桌面/移动共 8 项 E2E 测试通过。

## 阶段 14：非 UI 完成度审计与核心边界纠偏

状态：进行中，禁止进入皮肤与交互阶段

结果：

- 完成非 UI 逐领域审计并新增 `NON_UI_READINESS.md`。队伍、佣兵、宠物、地下城、完整挂机目标、CloudBase 服务端、真实 AI 传输层和全中文审计均明确保持未完成状态，不再用原型或接口文件代替阶段完成。
- 修复生产构建发现的重复导出、只读静态数据、联合类型和 Store 字段类型问题；此前“单独类型检查与 100 项测试通过，但生产构建失败”的工程门禁缺口已关闭。
- 新增纯 `legacy-equipment.commands.ts`，将六槽装备穿脱从 Pinia Store 移回 `game-core`，并纠正实验 Store 私自增加等级限制、六维调整和当前资源夹取的行为偏差。
- 新增纯 `legacy-combat.resolution.ts`，接管基础单人胜利、任务、契约、传承试炼、追猎、掉落、符文、普通/硬核死亡和逃跑结算；恢复冻结版 60% 逃跑成功率与硬核死亡删除角色槽位的合同。
- 新增 `GameCloudRepository`、CloudBase 云函数客户端与 P0 方法映射，统一结构化错误；新增 RunningHub 立绘任务边界和支持成功、失败、超时、取消的轮询器。当前只完成客户端契约，未宣称真实云端或第三方服务已联调。
- UI、CSS 和冻结单体均未修改；默认 `/` 继续提供旧版完整结构与交互，实验原型不接管玩家流程。

兼容性：装备和基础单人战斗的规则从 Store 下沉后保持冻结版字段与随机源注入边界；扩展战斗和服务端权威结算仍由完成度门禁阻止提前接管。

验证：`npm run verify` 通过，包含静态数据一致性、类型检查、20 个测试文件共 113 项单元/回归测试、生产构建，以及冻结单体 SHA-256 校验。

## 阶段 15：地下城、队伍与同伴纯核心

状态：基础完成，正式入口仍使用冻结版

结果：

- 修复地下城离线结算重复应用通用经验倍率的问题，并增加“2 倍修饰必须恰好得到 2 倍经验”的回归测试。
- 新增地下城离线模拟器，覆盖对数楼层倍率、每 5 层 Boss、主题/难度/赛季词缀、遗物、印记、精华、检查点、Boss 保底装备与自动深入限制；普通挂机入口会显式分派地下城目标。
- 将 `MERCENARY_TYPES`、`PETS` 和 `PHASE5` 纳入冻结单体静态生成门禁，避免后续手工复制数据漂移。
- 新增队伍纯核心：六人上限、保存阵型清洗、前后排、偷袭后排、目标权重、持续 HP 池、倒地与复苏治疗、佣兵挡伤、敌方群攻、本地/LFG 队友施法、队伍经验和本地角色金币分配。
- 新增同伴纯核心：主佣兵/队伍佣兵伤害、减益、治疗、回蓝、冷却、触发概率、六槽装备伤害加成、雇佣/工资与欠薪解约；恢复狼、鹰、恶魔和治疗灵体的行动及收益修饰。
- 新增怪物包纯核心：区域/地下城规模分布、目标切换、威胁等级、横扫、劈砍溅射、协攻、伤害上限和次要目标死亡清理。
- 角色工厂和迁移器新增 `group`、`lfgParty`、`partyLineup`、`partyHp`、队伍佣兵名称/装备等字段的默认值与清洗，保证旧存档在机制调整期间仍可稳定迁移。

兼容性：所有逻辑均无 DOM 依赖，使用注入随机源，且测试确认不会原地修改输入。默认 `/`、冻结单体、旧版 UI/交互和装备展示均未改动。

验证：阶段收口时 `npm run verify` 全量通过，包含静态数据一致性、类型检查、25 个测试文件共 142 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle 构建与冻结 SHA-256 校验；随后桌面/移动共 8 项 E2E 回归通过，确认默认入口、旧版本地模式和实验入口均可正常加载。

## 阶段 16：CloudBase 服务端准备层与延期决定

状态：源码准备完成，真实接入延期

结果：

- 为 10 个 P0 方法建立共用的服务端权威服务，覆盖身份边界、角色创建/读取/乐观并发保存、服务端挂机结算、装备命令、公开快照、确定性挑战、排行榜和立绘任务状态。
- 新增内存事务仓储和 7 项本地服务集成测试，覆盖槽位唯一性、保存冲突、挂机防重放、服务端装备属性、排行榜、跨账号挑战与结构化错误。
- 新增 CloudBase Node 事务适配、五个集合及索引清单、数据库/云函数安全规则、Node 20 bundle 构建、环境配置模板和手动部署流水线。环境 ID、凭据和密钥不入库。
- 2026-08-01 产品决定：游戏内部机制仍可能调整，CloudBase 不着急接入。现有准备层保持隔离，待机制定稿后再更新权威规则、连接客户端、部署并执行真实环境端到端联调。

兼容性：CloudBase 代码没有被本地 Store 或默认入口引用，不改变离线游戏行为；不能将“源码准备完成”描述为“已接入、已部署或已上线”。

## 阶段 17：机制调整准备层与完整回归

状态：已完成准备，等待玩法调整；CloudBase 继续延期

结果：

- 新增装备强化纯命令，覆盖自动换装评分、打孔成本、符文消耗、符文之语识别、符文销毁、重铸范围/价格与装备中属性同步；修正武器触发查询，使后期扩展武器与冻结版 `Object.assign` 后的完整表一致。
- 新增背包容器纯命令，覆盖四包默认值、实际容量、溢出分组、商店价格、容器替换和降容量保护；核对冻结版不存在双手武器与副手互斥规则，不额外发明限制。
- 新增战斗效果纯核心，覆盖角色状态清理/叠乘、怪物持续伤害与眩晕、800ms 全局冷却、武器伤害/回蓝/毒伤/削弱触发、符文吸血，以及命名怪狂暴、护盾轮次与毒液概率。
- 新增功法状态与购买命令，覆盖声望要求、商店价格、已知功法清洗、二至六记忆槽、记忆/清除；治疗功法可选择血量比例最低的队伍成员并复苏倒地单位。
- 新增存档影子对比器，提供字段路径级的类型、数值、缺失和新增差异，支持显式忽略瞬态字段及差异数量上限，便于机制字段调整时审查兼容性。
- 新增 `GAMEPLAY_CHANGE_GUIDE.md`，明确各类机制的纯规则入口、对应测试、生成数据边界、完整验收顺序和恢复 CloudBase 对接前的检查点。
- 本阶段未修改 UI、CSS、交互结构或冻结单体；CloudBase 只参与隔离源码构建，没有连接客户端、部署或访问真实环境。

兼容性：默认 `/` 继续加载旧版完整界面，战斗场景、角色/怪物画面、六槽装备展示与比较交互均保持原样；所有新增命令均返回新快照，不原地修改输入存档。

验证：`npm run verify:full` 全量通过，包括静态数据一致性、类型检查、30 个测试文件共 169 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle 构建、入口 15 行与冻结 SHA-256 校验，以及桌面/移动共 8 项 E2E 回归。

## 阶段 18：修仙化中文显示层（按话术参考）

状态：第一阶段完成

结果：

- 读取项目提供的《凡修录》汉化与修仙化命名话术参考，建立独立 `src/game-core/data/localization.zh-cn.ts`；内部英文 ID、存档字段、规则数值和装备槽合同保持不变。
- 按参考完成第一阶段显示术语：修为等级、修为、灵石、气血、法力、会心率、法力恢复、调息时间、角色库、储物袋、随身装备、功法典籍、斗法、自动历练、闭关挂机、委托、秘境、坊市、公共仓库，以及 16 个灵根/体质、16 个初始传承、六维属性、品质、装备槽和初始术法名称。
- 将原生实验入口的散落术语统一到该显示层，修正原生战报中的资源、战利品、退走、术法和等级话术；没有改变战斗结算、装备比较或面板结构。
- 默认旧版仍加载冻结 `legacy/fanxiulu-monolith.html`，新增 `legacy-text-bridge.ts` 在 iframe 外层只处理文字节点和标题/占位符/ARIA/替代文本。它额外把冻结脚本产生的具体境界显示归一为“修为等级 N”，以遵守玩法定稿前不改变等级语义的约束。
- 原生成 `localization.generated.ts` 未手工编辑，`data:verify` 仍负责确认其与冻结单体一致；词表未写回角色或装备数据。

兼容性：冻结单体 SHA-256 不变；默认旧版的场景、角色/妖兽画面、纸娃娃六槽、背包、装备对比、战斗和交互路径不变。CloudBase 真实对接继续延期，等待游戏机制调整定稿。

验证：阶段收口时 31 个测试文件共 172 项单元/回归测试、Vue 生产构建，以及桌面/移动共 8 项 E2E 回归通过。

## 阶段 19：旧版入口修复与内容名称第二阶段

状态：已完成

结果：

- 修复中文桥接导致旧版登录页延迟卡住的问题：撤掉父页面新增的常驻 `MutationObserver`，避免与冻结单体自带观察器竞争改写；显示覆盖只在 iframe 加载完成及异步公告返回后执行两次有限遍历，并保留节点幂等缓存。
- 为公告较多和短窗口场景增加兼容样式，只让旧登录页自然滚动并限制公告列表高度；没有改变登录卡片结构、按钮顺序或进入流程。旧版仍要求玩家点击“本地进入”后进入角色库，不擅自改成自动跳转。
- E2E 公告夹具由空列表改为 5 条公告，覆盖此前未被测试发现的真实入口场景；桌面与移动测试都会实际点击“本地进入”并确认旧版角色库激活。
- 新增独立 `localization-content.zh-cn.ts`，完成内容名称第二阶段：区域及说明、普通妖物、命名强敌、世界异象、秘境主题、材料、装备、随机词缀、极品装备、套装、武器触发、任务、功法名、传承试炼、符纹与坊市丹药均通过显示层修仙化。
- 新增静态内容名审计，遍历生成的区域、怪物、任务、装备、词缀、套装、武器触发、功法、传承试炼、符纹和符纹造物，确保玩家可见名称不再残留英文；内部 ID、数值、掉落、存档字段和规则数据保持原样。

兼容性：冻结单体没有修改，默认旧版场景、角色/妖兽画面、纸娃娃六槽、背包、装备比较、战斗结构和交互合同保持不变；CloudBase 继续延期，UI/皮肤仍留到最后统一设计。

验证：`npm run verify` 通过，包含静态数据一致性、类型检查、31 个测试文件共 173 项单元/回归测试、生产构建、CloudBase 隔离 bundle 和冻结 SHA-256；桌面/移动共 8 项 E2E 全部通过。另在真实 5 条公告页面中实际点击“本地进入”，旧版角色库成功切换为激活状态。

## 阶段 20：参考话术重做与无冲突动态中文化

状态：历史实现，已由阶段 21 的单源中文运行副本替代

结果：

- 将参考文档中的权威术语从历史兼容大表中拆出，按 `common`、`characters`、`skills`、`items`、`inventory`、`world`、`creation`、`combat`、`afk`、`interface`、`errors` 分类放入 `src/game-core/locales/zh-CN/`；权威词库高于历史生成映射和内容扩展映射，内部英文 ID、规则和存档字段不变。
- 增加 `npm run lint:locale`，锁定资源、16 个灵根/体质、16 个初始传承、六维属性、代表技能、装备槽、品质、区域/任务、系统错误和内容名称；语言包输出不得残留玩家可见英文。
- 重写冻结页桥接安全边界：禁止句内子串替换，只允许整标签精确匹配、跨行完整标签空白归一、公告类别及角色签名/时间戳/进度等明确动态格式；桥接保持幂等，不修改旧版事件、表单值、布局或内部脚本。
- 继续禁止第二个常驻 `MutationObserver`。加载后只执行有限遍历；角色页、教程和移动弹窗等异步界面通过用户交互后的 50ms/850ms 两次去抖遍历处理，完成后停止，不与冻结单体自带观察器竞争。
- 按参考补齐登录页、角色库、角色创建、六维属性、主游戏首屏、战斗控制、挂机目标、教程、版本弹窗、背包筛选/排序/批量操作、装备纸娃娃、无障碍标签和动态组合文本；旧版纸娃娃、战斗场景、装备比较和所有交互结构保持不变。
- 新增长等待正式入口回归：加载 5 条公告后等待再进入角色库；使用既有角色进入主游戏，等待两次处理结束后推进原版教程并确认背包面板显示，覆盖此前延迟卡死和点击失效路径。

兼容性：`legacy/fanxiulu-monolith.html` 未修改；CloudBase 真实接入继续延期，UI/皮肤仍留到最后。默认 `/` 保持旧版完整场景、角色/妖兽画面、六槽装备、背包与对比交互。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、32 个测试文件共 177 项单元/回归测试、生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 10 项 E2E；真实浏览器在登录页等待 12 秒后仍可依次进入角色库和主游戏。

## 阶段 21：取消整页映射并生成单源中文运行副本

状态：已替代阶段 20 的交互后文本桥方案

结果：

- 根据真实页面出现的“点击后切换译名”“角色槽先显示开辟新道途再变成创建角色”和 Loading 卡住问题，确认根因是冻结页原有映射与父页面二次映射同时工作，以及扩大词表后整页扫描成本过高。
- 删除 `legacy-text-bridge.ts`。父 iframe 现在只保留小屏登录页滚动保护，不再遍历、监听或改写任何文字。
- `legacy-runtime-source.ts` 从 SHA 固定的冻结源生成隔离运行副本：普通标题、按钮、页签和说明文字在构建时直接写入最终中文；角色、职业、装备等兼作规则键和存档值的英文 ID 继续保留，只在具体 DOM 显示写入时同步转换。
- 移除运行副本中的整页 `MutationObserver`、80/500/1800ms 延时 `applyAll` 和点击/提交后的补翻译。页面显示后不再有另一套任务修改同一文字。
- 保留参考文档的最新权威词库，不回退到冻结版旧话术；角色库固定显示“创建角色”，六维固定为“根骨、身法、体魄、悟性、神识、机缘”。
- 基线检查改为同时验证冻结源 SHA 不变、构建产物确为独立中文副本、旧映射脚本不存在、文本观察器数量为零。
- E2E 新增角色库 24 个空槽稳定性和主游戏职业/六维点击前后相等断言；桌面完整 5 条流程已通过，包含长等待、进入角色库、进入主游戏、推进教程与打开旧版装备/储物袋。

兼容性：没有修改 `legacy/fanxiulu-monolith.html`，没有改变旧版 UI、战斗场景、纸娃娃装备、背包比较、事件结构、规则或存档字段；CloudBase 与最终皮肤/交互设计仍按产品决定延期。

验证：`npm run verify:full` 通过，32 个测试文件共 177 项单元/回归测试及桌面/移动 10 项 E2E 全部成功；真实 5173 页面等待后可连续进入角色库和主游戏，角色槽、职业、六维、登录奖励、升级提示及挂机目标首次显示即为最终中文。

## 阶段 22：版本化本地存档信封与原生切换影子门禁

状态：已完成

结果：

- 新增 `fanxiulu-native-save` v1 纯数据存档信封，固定格式版本、游戏版本、存档 schema、创建时间、活动槽、旧版本地存档来源哈希、24 槽载荷和完整载荷哈希。
- 新增信封解析与校验，明确拒绝缺失、无效 JSON、无效根结构、不支持的格式/版本/schema、无效时间或槽位、无效来源、非法槽数组和载荷哈希不一致。
- 新增纯 `evaluateNativeRuntimeGate()`：只有用户明确请求原生实验入口、旧档可完整解析、候选信封有效、来源哈希未过期且字段级影子对比完全一致时才允许切换。
- `featureFlags.ts` 已实际接入门禁。`/?native=1` 不再只凭查询参数直接进入；任何损坏、篡改、过期或字段差异都会自动保留默认旧版。
- 当前入口只在内存中生成兼容候选信封并执行只读检查，不持久化信封、不写回或覆盖 `EmberQuest_slots`，也不改变默认 `/`、旧 UI、交互、战斗场景或装备比较。
- CloudBase 继续按产品决定延期；本阶段没有连接、部署或改动云端权威规则。

兼容性：冻结单体没有修改，旧存档字段和英文内部 ID 保持不变。后续玩法字段调整必须同步迁移器、schema、信封和影子差异测试，不能绕过门禁直接接管玩家流程。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、34 个测试文件共 187 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 10 项 E2E。

## 阶段 23：自动施法优先级与结构化战报回放

状态：已完成

结果：

- 对照冻结版 `afkTick()` 和 `castMemorizedSpell()`，新增纯自动施法规划器：按记忆槽 1→6 依次尝试，跳过总开关关闭、单槽关闭、空槽、未知功法、冷却、法力不足和敌对功法缺少目标，首个可用功法成功后结束本次挂机动作。
- 保留冻结版“自动施法不占用手动 800ms 全局调息”的合同；规划阶段不消耗随机数、不修改角色或目标，只有选中功法后才调用纯施法引擎。
- 角色工厂和旧档迁移器补齐冻结版既有的 `autoUseSkills` 与六项 `autoSkillSlots` 默认值；原生 `AfkStore` 在药品规则之后、普通攻击之前调用自动施法。
- 新增 v1 结构化战斗事件日志，覆盖遭遇、攻防命中/闪避、眩晕、施法、恢复、任务、契约、掉落、符纹、奖励、胜负和退走；每条事件与现有中文战报共享序号和消息。
- 新增战报 JSON 导出、严格解析、顺序校验、最多 5,000 事件保护、恢复最近 80 条，以及伤害、承伤、治疗、回蓝、修为、灵石、胜负、施法和掉落的纯汇总回放。
- 原生 `CombatStore` 已接入事件日志；现有界面继续只显示同一条中文消息，没有改动旧版或原生实验界面的布局、按钮、交互与样式。

兼容性：冻结单体和默认 `/` 未修改。新增的自动施法字段本来就是冻结版存档字段，内部功法 ID 和现有存档语义不变；CloudBase 继续延期。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、36 个测试文件共 197 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 10 项 E2E。

## 阶段 24：命名强敌回合链与区域首领核心

状态：已完成

结果：

- 对照冻结版 `doAttack()` 锁定命名机制顺序：玩家伤害与护盾吸收、40% 气血狂暴、敌方回合递增、眩晕整回合跳过、普通反击、毒液追加、每第 3 个有效回合刷新护盾。
- 新增纯 `resolveLegacySoloMobTurn()`，统一普通怪、命名强敌和首领反击；狂暴在反击前生效，毒液保持冻结版“普通反击被闪避后仍可能触发”的行为，眩晕回合不刷新护盾。
- 结构化战报扩展狂暴、毒液、护盾刷新和护盾吸收事件；原生战斗 Store 已接入回合编排器并记录对应中文消息。
- 将冻结版 19 个 `BOSS_BY_ZONE` 配置纳入静态数据生成门禁；后续区域继续按冻结逻辑生成使用区域首只妖物和护盾机制的兜底首领。
- 新增纯区域首领工厂，覆盖当前战斗/秘境限制、按区域冷却、玩家等级 +2、冻结 HP/攻击/防御公式和命名机制初始状态。
- 首领胜利结算新增 30 分钟区域冷却、保底区域稀有装备与 18% 传说装备；额外掉落进入角色背包、结构化战报和现有首领击杀统计。
- 原生 `CombatStore` 暴露 `spawnBoss()` 供后续等价界面使用，但本阶段不增加按钮、不改变默认旧版或实验界面结构。

兼容性：冻结单体、默认 UI、交互与存档字段未修改；首领冷却继续使用冻结版既有 `bossCooldowns` 字段。CloudBase 真实对接与皮肤设计继续延期。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、38 个测试文件共 206 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 10 项 E2E。

## 阶段 25：本地队伍离线修为分发

状态：已完成

结果：

- 对照冻结版 `awardExperience()`、`distributeGroupXp()` 和 `simulateAfkReturn()`，确认离线结算先按队伍总人数与队伍加成计算每人修为份额，再将同额修为发给每个有效本地队友。
- 确认并锁定冻结版差异：在线击杀可按 `groupSplitGold` 分金，但离线回归直接把全部灵石加入当前角色，不向队友分发。
- 将原队伍战斗中的共享修为应用器公开为纯函数，继续保持队友不走队长 AA 分流、每名队友独立升级并获得职业成长属性的旧合同。
- 新增 `simulateLegacyPartyAfkReturn()`，从完整 24 槽存档建立六人阵型，把佣兵、本地角色和 LFG 英雄计入队伍份额，并只向相同生死道的有效本地角色槽发放修为。
- 纯结算返回队伍人数、更新后的全部槽位及每个本地队友的修为/升级摘要；无效活动槽直接拒绝，输入存档不原地修改。
- 原生 `AfkStore.recoverOffline()` 已改为使用队伍离线结算并一次回写全部槽位，避免只保存队长而丢失队友修为。

兼容性：默认旧版入口、UI、交互和存档字段不变；离线分发只使用冻结版已有 `group`、`partyLineup`、`hardcore`、`xp` 和成长字段。CloudBase 继续延期。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、39 个测试文件共 211 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 10 项 E2E。

## 阶段 26：显示文本生成期直写与全局拦截移除

状态：已完成本阶段

结果：

- 对照话术参考第 1.3 节，确认不能把 `Warrior`、`Human`、装备名等兼作规则键和存档值的内部 ID 全局改为中文；普通按钮、标题、说明和动态模板中的显示字面量则应在生成运行副本时直接写成目标中文。
- `legacy-runtime-source.ts` 新增构建期脚本分析：定位 `textContent`、`innerText`、`innerHTML`、标题、无障碍属性、Toast、战报和浏览器提示等明确显示边界，直接替换其中的显示字面量，并为仍来自内部 ID 的表达式加上局部同步显示转换。
- 删除上一阶段仍存在的 `Node`、`CharacterData`、`Element`、`HTMLElement`、`HTMLOptionElement`、`HTMLInputElement` 和 `Document` 原型拦截。运行副本不再改写浏览器原生 setter，不会因任意组件写入或点击而触发全局文本处理。
- 角色库空槽在生成后的游戏代码中直接写为“创建角色”；设置、秘境、功法、同行队伍、战报工具、储物袋和坊市等高优先级隐藏面板补齐参考话术，新增 `settings`、`dungeon`、`social` 分类词表。
- 基线门禁新增“禁止 DOM 原型覆写”检查；单元测试会剥离脚本和样式审计静态玩家文案，并锁定角色槽和动态输出均走明确显示赋值。
- 新增桌面/移动隐藏面板回归：实际进入旧版主游戏，打开设置并收起/展开，再往返功法、同行队伍和秘境，确认首次打开即为目标中文、再次打开文本完全相同且没有页面异常。
- E2E 对 Supabase CDN 使用只读登录态替身，消除外部 CDN 偶发失败导致测试直接进入本地角色库的非确定性；不改变游戏运行代码和 CloudBase 延期决定。

兼容性：冻结单体、默认旧版 UI、战斗场景、角色/妖兽动画、六槽纸娃娃、背包与装备比较交互均未修改。内部英文 ID、规则、数值和存档字段保持原样；CloudBase 真实接入与最终皮肤/交互设计继续延期。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、39 个测试文件共 212 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 12 项 E2E。

## 阶段 27：角色身份中文正式 ID 与旧档双向适配

状态：已完成本阶段

结果：

- 根据产品决定，新游戏核心不再把 `Human`、`Warrior` 等英文值作为长期领域 ID。新增 `game-core/domain/character-identity.ts`，以“五行杂灵根”“炼体士”等参考话术名称定义 16 个灵根/体质和 16 个初始传承的正式中文 ID。
- 建立完整双射和类型守卫：中文 ID 可在冻结数据边界转换为旧英文键，旧英文 ID 只在一次性导入时转换为中文；同时生成中文灵根—传承限制表，保持冻结版全部可选范围不变。
- 新增角色身份存档适配器。原生 `SaveStore` 加载后只持有中文 ID，角色创建直接生成中文领域状态；写入 `EmberQuest_slots` 和旧备份前自动导出英文兼容值，避免访问实验入口后破坏默认旧界面。
- `fanxiulu-native-save` 从 v1 升级为 v2，并使用独立原生 schema；信封角色槽保存中文身份 ID。影子门禁在与旧档比较前导出兼容值，因此仍能严格发现其他字段差异。
- 原生角色库不再调用显示翻译映射，选项值与显示文本都是同一个中文 ID；技能书查找、自动施法和升级成长支持中文 ID，数值仍读取冻结生成数据。
- 新增映射完整性、双射、灵根—传承限制等价、旧档导入、中文原生信封往返、旧档导出、技能书与成长数值等价测试。

兼容性：没有修改 `legacy/fanxiulu-monolith.html` 或任何冻结生成数据；默认旧 UI、战斗场景、角色/妖兽动画、纸娃娃六槽、背包和装备比较不变。CloudBase 真实对接继续延期，皮肤与交互设计仍留到最后。功法、装备、怪物和区域 ID 将按同一模式分阶段迁移，不能用无边界的全局替换破坏旧档。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、40 个测试文件共 216 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 12 项 E2E。实验入口可用中文 ID 创建并重载角色；默认旧入口长等待后仍可进入主游戏并稳定切换隐藏面板。

## 阶段 28：完整内容词库接入与动态中文句式稳定化

状态：已完成本阶段

结果：

- 修复中文运行副本只合并基础话术、未实际载入完整内容词库的问题；区域、怪物、命名强敌、装备、随机词缀、套装、功法和任务等内容名称现在统一进入生成后的默认旧版运行时。
- 动态文本改为“片段精确转换 + 完整句式转换”：战斗日志、离线收益、契约追踪、区域选择、储物袋统计、坊市服务和赌场说明会在实际显示写入边界一次生成目标中文，不依赖点击、延时扫描或 DOM 观察器补翻译。
- 收紧通用片段替换范围，排除 `the`、`for`、`of`、`and` 等连接词和带首尾空白的危险短键，避免出现中英文词序拼接、重复词和点击前后换词。
- 对多义词增加静态上下文规则：角色容量中的 `Slots` 固定为“道册位”，赌场入口中的 `Slots` 固定为“灵轮机”，不再使用会互相覆盖的全局词义。
- 补齐截图暴露的战斗记录、离线结算、区域/契约面板、生成装备名与词缀、储物袋统计、坊市及其服务弹窗、赌场主面板；同时扩充关于、社群、斗法台、宗门、行动队列、符纹和外观等低频面板话术。
- 端到端回归不只检查静态页面：会实际打开坊市服务、生成妖物并攻击，检查动态战报；另对离线返回、赌场欢迎语、储物袋统计、动态装备名和阵营说明进行确定性验证。

兼容性：没有修改 `legacy/fanxiulu-monolith.html`。默认旧版布局、按钮、交互、战斗场景与动画、六槽纸娃娃、背包格和装备比较均保持原样；本阶段只修改隔离运行副本的显示文本。CloudBase 真实接入继续等待游戏机制定稿。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、40 个测试文件共 216 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 12 项 E2E。

## 阶段 29：战斗群攻、重修/飞升与完整悟道中文化及新玩法架构审计

状态：已完成本阶段

结果：

- 补齐战斗中“切换目标”“轮换目标”“横扫敌群”等拼接式战报的精确中文片段，继续在战报写入边界同步生成完整中文，不增加 DOM 扫描、观察器或点击后补翻译。
- 新增独立 `progression` 权威词表，完整覆盖重修、飞升、重修天赋、悟道分流、初始传承、通用/流派/传承/终极分支、全部下方节点、模拟器、预设和方案比较，而不是只翻译弹窗首屏。
- 为等级、点数、下一阶收益、悟道投入、修为分流等动态数值句式增加确定性转换；修复文本节点被组件拆分时遗留的 `Flat`、`Choose`、`Rank` 等英文片段。
- 端到端测试使用 129 级丹医夹具实际打开“重修与飞升”和完整“悟道”面板，在桌面端与移动端检查全部可见文本均不存在连续英文字母；同时覆盖用户截图中的目标切换和横扫战报。
- 完成面向新玩法的架构审计：装备、功法、战斗结算、队伍、首领、挂机、离线和随机数等已有纯核心可作为回归基座，但默认 `/` 仍由 Vue iframe 加载旧版隔离运行副本，原生存档领域仍存在宽泛字段，现有战斗编排仍是玩家先手模型，因此当前不能称为彻底模块化或最终新玩法架构。
- 更新 `GAMEPLAY_CHANGE_GUIDE.md`：后续先建立强类型中文领域模型与版本化规则集，再实现带阶段、行动队列和可注入随机数的回合制状态机，随后接入五行相克、灵根/天赋成长、修行玩法与无皮肤的原生功能等价壳；默认入口切换完成后，冻结 HTML 只保留为测试夹具和行为参考。
- 更新非 UI 门禁，明确 CloudBase 继续等待玩法定稿；最终皮肤和交互设计仍在规则架构、原生功能等价和默认入口脱离 iframe 之后进行。

兼容性：没有修改 `legacy/fanxiulu-monolith.html`。旧版布局、战斗场景和动画、六槽纸娃娃、背包格、装备详情与比较交互均保持不变；本阶段只修复隔离运行副本的显示文本、测试和维护文档。CloudBase 未部署、未联调。

验证：`npm run verify:full` 全量通过，包含静态数据一致性、类型检查、40 个测试文件共 216 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle、冻结 SHA-256，以及桌面/移动共 12 项 E2E。

## 阶段 30：完整游戏机制、玩法与页面元素档案

状态：已完成本阶段

结果：

- 新建 `GAME_MECHANICS_CATALOG.md`，作为后续调整玩法、删减功能、重做 UI、设计新修行系统和迁移存档时的单一分析底稿。
- 按真实玩家路径记录登录、角色库、四步创建、桌面/移动公共外壳、6 个主标签、全部弹窗/二级页面、设置、低频覆盖层、赌场子界面、LFG、云冲突和管理员工具所包含的元素、按钮、状态与数据影响。
- 记录角色/成长、战斗、区域、首领、装备、符纹、炼丹、功法、队伍、护道者、灵兽、任务、契约、秘境、挂机、离线结算、悟道、转世重修、经济和社交的现有规则；内容量清单包含 41 个区域、123 种常规妖物、82 个命名强敌名称池、96 本标准功法、16 本史诗功法、26 条普通悬赏、15 项成就、14 个丹方等当前基线。
- 明确区分“现有真实机制”和“尚未实现的候选玩法”：当前没有五行克制、元素抗性、身法先手、行动条/回合制、灵根成长或境界突破流程，避免后续设计时误把规划当成既有规则。
- 增加新玩法影响地图和改动前检查清单；把用户规划的属性相克、身法先手回合制、灵根/天赋成长、新修行玩法、功能删减与最终 UI 重做，分别映射到规则、页面、存档和离线结算的改动范围。
- 同步说明模块化真实边界：默认 `/` 仍使用冻结旧版作为完整运行时，`src/game-core` 中的纯规则是未来新玩法的正确落点，`/?native=1` 仍是受影子门禁保护的实验入口；CloudBase 继续等玩法与存档定稿后再接入。

兼容性：本阶段仅新增/更新维护文档；未修改冻结 `legacy/fanxiulu-monolith.html`、旧版 UI、交互、战斗场景、存档、CloudBase 或任何游戏规则。

验证：完成静态数据、页面入口与已抽取核心的交叉盘点；后续仅运行文档空白/链接检查和冻结 SHA-256 校验，因无运行代码变动不重复执行全量构建/E2E。

## 阶段 31：P0 原生功能等价路线与三模式入口

状态：已完成第一批基础设施，P0 总阶段继续进行

结果：

- 根据 `凡修录_P0_原生功能等价壳与冻结基线.md` 新建 `P0_IMPLEMENTATION_PLAN.md`，将 P0 拆分为冻结基线、原生应用壳、Application Service、角色/存档、核心页面、冻结外围和默认入口切换七个可独立回归阶段。
- 新建 `P0_FEATURE_MATRIX.md`、`P0_FROZEN_FEATURES.md` 和 `P0_KNOWN_DIFFERENCES.md`，明确当前完成度、阻断缺口、只读兼容范围和默认原生入口必须隐藏的外围功能。
- 新增 `legacy/baseline-version.json` 和 `legacy/baseline-behavior-report.md`，固定提交锚点、版本、存档 schema、冻结单体及角色/世界/装备/功法/任务/符纹静态数据 SHA-256。
- 建立 `legacy/baseline-save-samples/` 与 `legacy/baseline-screenshots/` 目录及采集清单；代表存档和截图将在对应 P0 阶段生成，不以占位样本冒充已验收基线。
- 运行入口开始支持 `/?mode=legacy`、`/?mode=equivalent` 和 `/?mode=v2`；旧 `/?native=1` 继续兼容并映射到 equivalent，显式 `mode` 优先。
- equivalent 继续受旧存档影子门禁保护；门禁失败自动回到 legacy。v2 只显示开发占位，不加载 iframe，也不开放任何新玩法。
- P0 迁移期间默认入口仍为 legacy；等功能矩阵全部通过后，只修改 `DEFAULT_RUNTIME_MODE` 即可将默认入口切到 equivalent。
- 基线脚本改为读取版本元数据并校验全部冻结静态数据、版本/schema、基线目录和行为报告，不再只校验单体 HTML。

兼容性：冻结单体、旧规则、存档字段、默认入口、UI 和 CloudBase 均未改变；本阶段只增加 P0 规划、基线门禁和显式运行模式。

验证：静态数据一致性检查、类型检查、40 个测试文件共 218 项单元/回归测试、Vue 生产构建、CloudBase 隔离 bundle 和扩展后的冻结基线门禁均通过。应用内浏览器实际验证 `equivalent` 与旧 `native=1` 都进入无 iframe 的原生角色库、`legacy` 只加载一个冻结 iframe、`v2` 只显示开发占位，且无控制台错误。Playwright 代码已新增 v2 回归，但本机 Playwright Chromium 可执行文件缺失，14 项 E2E 在浏览器启动前失败，未产生页面逻辑失败。


按依赖顺序连续执行：

1. 角色存档迁移与字段清洗。
2. 装备、功法、战斗、掉落、挂机和离线结算核心。
3. Pinia 领域状态与新旧运行时切换。
4. Vue 角色、背包、战斗和挂机界面。
5. 本地、Supabase 兼容、CloudBase 与 AI 立绘仓储。
6. 全中文、响应式、可访问性和完整 E2E 审计。
7. 移除旧 DOM 运行时并完成最终验收。

## 阶段 32：P0 原生功能等价壳收口与旧 UI/交互恢复

状态：已完成

边界：严格依据 `凡修录_P0_原生功能等价壳与冻结基线.md`；不进入 P1，不修改冻结单体，不接入 CloudBase 权威结算。

结果：

- 完成 P0-0 至 P0-6：默认 `/` 已切换为无 iframe 的 Vue 原生等价壳；`/?mode=legacy` 继续访问冻结旧版，`/?mode=v2` 仍为开发占位。
- 建立并接入 `LegacyBattleApplication`、`LegacyAfkApplication`、`LegacyProgressionApplication` 和 `LegacyEquipmentApplication`，固定 Vue → Pinia → Application Service → `game-core` → Pinia 保存 → Vue 渲染的调用链。
- 修复响应式角色存档在装备、区域和首领操作中被 `structuredClone()` 拒绝的问题；装备应用服务改为旧存档可序列化深复制，并增加响应式输入回归。
- 补齐 24 槽角色库、四步角色创建、删除确认、旧档读取、普通/加密/单角色导出、自动备份、导入预览、冲突提示和损坏存档隔离。
- 补齐角色状态、区域倍率/绑定/猎杀目标/首领、旧顺序战斗、六槽装备、背包分页/搜索/排序/筛选/比较/锁定/收藏/出售/自动装备、功法冷却/记忆/自动施放、挂机方案和离线收益。
- 冻结赌场、聊天、宗门、Discord、管理员和 LFG；悟道、重修/飞升、符纹、同行、护道者、灵兽、旧炼丹、神龛和套装收藏只读兼容，且不注册后台推进逻辑。
- 冻结基线补齐四类存档样本、版本/schema/静态数据哈希、行为报告和 13 张桌面/移动截图；默认完成态记录为 `equivalent`。

UI/交互纠正：

- 发现第一版原生壳擅自加入三栏新视觉系统、自绘大型 SVG 战斗角色/妖兽、新式装备卡片和不同的角色库布局，超出 P0“功能等价、不换皮、不重构旧交互”的边界。
- 以 GitHub 提交 `07919d3` 和冻结 `legacy/fanxiulu-monolith.html` 的实际运行画面为唯一基准，撤销越界设计。
- 原生壳恢复旧式登录卡、角色库左侧列表与中央角色舞台、暗绿/金色 RPG 面板、顶部工具条、桌面左角色栏—中标签页—右挂机栏、蓝天绿地战斗场景、角色/妖兽画面、六槽纸娃娃和点击物品后并排装备比较。
- 移动端继续保留 P0 要求的底部核心导航和无横向溢出；冻结外围入口不因视觉恢复重新开放。
- 新增明确门禁：P0/P1 规则开发不得再次混入换皮或交互重做；最终 UI 必须由产品在独立阶段统一设计。

兼容性：

- `legacy/fanxiulu-monolith.html` 未修改，冻结 SHA-256 继续为 `113F9ED589375E746C50A13EC6FE29B32BC6F255B208B7FACE43D7218A2685CC`。
- 旧规则、旧数值、旧装备槽、旧存档未知字段和内部兼容 ID 均保留；未实现回合制、身法先攻、五行相克、灵根亲和、境界突破或三功法限制。
- CloudBase 未部署、未联调。

验证：

- TypeScript 类型检查通过。
- 42 个测试文件、228 项单元/回归测试通过。
- Vue 生产构建通过。
- P0 桌面深度流程与 390px 移动端流程通过；全量桌面/移动 E2E 使用系统 Edge 验收，19 项通过、3 项按设备条件跳过、0 项失败。
- 装备比较、锁定、收藏、出售、六槽装备、功法冷却、自动施法、挂机方案、离线收益、中文区域首领、默认无 iframe 和冻结 legacy 入口均有自动化覆盖。

## 阶段 33：P1 身法先攻回合制与五行构筑

状态：已完成

边界：严格依据 `凡修录_P1_回合制战斗与五行构筑.md`；在 P0 原生等价壳上新增版本化 `ruleset v2`，不修改 UI 风格、布局骨架和旧交互，不修改冻结单体，不进入长期成长、多人战斗、正式联网或最终皮肤阶段。

规则与架构：

- 新建 `src/game-core/rulesets/v2/`，实现五行、灵根亲和、九种属性抗性、身法先攻、三功法配置、功法类型、冷却、状态时机与完整伤害分解；Pinia 和 Vue 不保存或复制这些公式。
- 完成 1v1 `BattleStateMachine`、`BattleCommand`、`BattleEvent`、固定 seed 随机源与 Manual、Auto、Monster、Test 四种策略。每轮重新按当前身法排序，双方最多各行动一次；眩晕/冻结可跳过行动，降身法影响下一轮，死亡立即终止后续行动。
- 普通攻击、三门功法、调息和遁走统一经过状态机验证；功法佩戴、法力、冷却、控制状态、行动者和目标合法性均不能绕过。手动与自动战斗使用同一状态机，固定角色/敌人/功法/seed 可复现完整事件序列。
- 伤害管线固定为基础值、属性缩放、灵根亲和、五行克制、目标抗性、防御、会心、状态和装备修正，并返回 UI 可解释的 `DamageBreakdown`。五行只启用金木水火土克制；雷、冰、风、暗只作为属性和抗性，不加入元素反应链。
- 新增 `V2BattleApplication` 和 `V2EquipmentApplication`，固定 Vue → Pinia → Application Service → ruleset v2 → BattleState/BattleEvent → Pinia 存档 → Vue 渲染的调用链。legacy 与 v2 在角色 `ruleset`、应用服务、事件和存档边界彻底分流。

内容与存档：

- P1 内容子集已落地：4 个传承、5 种灵根、5 个区域、15 种普通妖兽、5 个精英/命名强敌、2 个区域首领、12 门功法和 29 件装备白名单；其余旧内容保留但在 v2 中禁用或标记为 `P1 未开放`。
- 新角色默认使用 v2；旧角色缺少 `ruleset` 时按 legacy 导入。修复灵根/传承组合被旧种族职业限制误拦截的问题，使 v2 创建只遵守 P1 内容规则。
- legacy 导出时移除仅用于内存分流的 `ruleset: legacy`，保证旧存档精确往返；v2 存档保留 `ruleset`、灵根亲和、抗性、三功法配置和新增战斗字段。刷新会保存角色气血、法力与已结算奖励；进行中的单场战斗按已知差异放弃，不破坏角色存档。

旧 UI 原位接入：

- `CombatPanel` 保留旧蓝天绿地场景、角色和妖兽画面、原按钮区与战斗日志，只在原位置增加回合、当前行动者、属性、抗性、克制、普通攻击、三功法、调息、遁走、单场自动战斗和最近伤害分解。
- `SpellsPanel` 对 legacy 保留旧功法快捷栏；v2 只显示三个出战槽及属性、法力、冷却、类型、亲和与可用状态，战斗中锁定并在战斗外更换时确认。
- `CharacterStatusRail` 增加 v2 规则标记、灵根亲和、九种抗性和战斗状态；`EquipmentPanel` 与 `InventoryPanel` 保留六槽纸娃娃和原装备并排比较，并增加 P1 抗性贡献、差值和未启用装备提示。
- `ZonesPanel` 在 v2 只开放前五区、五个精英和两个首领；`AfkControlRail` 在 v2 禁止 legacy 长期挂机、离线收益和循环，只保留当前单场的自动战斗。浏览器实景确认 legacy 角色仍使用 P0 恢复后的暗绿/金色三栏与原场景，没有换皮或重做布局。

冻结范围：

- v2 不读取或推进悟道、转世、飞升、无尽节点、符纹、套装、灵兽、同行、护道者、神龛、赌场、宗门、旧挂机和离线经济等长期加成。
- 未实现玩家队伍、多妖兽包、多目标、群攻、群疗、复活、行动条、一轮多动、插队、反击、追击、连携、第四功法槽、元素反应、境界突破、经验曲线调整、天赋树、洞府、炼丹、PvP、公会或正式 CloudBase 对接。
- `legacy/fanxiulu-monolith.html` 未修改；旧角色继续运行旧战斗、旧功法、旧挂机、旧首领和旧装备公式。legacy 与 v2 公式不会在同一场战斗中混用。

验证：

- 冻结静态数据提取一致性检查通过；TypeScript `--noEmit` 与项目引用构建均通过；Vue 生产构建与 CloudBase 隔离 bundle 构建通过。
- 46 个单元/回归测试文件、261 项测试全部通过，覆盖五行/抗性/亲和边界、身法先攻、回合状态机、三功法、固定 seed 回放、内容子集、应用服务、存档分流、legacy 精确往返和 P0 基线。
- 全量 Playwright 使用系统 Edge 验收：21 项通过、5 项按桌面/移动条件跳过、0 项失败；P1 桌面流程覆盖创建 v2 角色、旧场景回合战斗、三功法、区域、装备比较和刷新恢复，legacy 桌面/移动流程继续通过。
- 冻结基线验收通过，`legacy/fanxiulu-monolith.html` SHA-256 仍为 `113F9ED589375E746C50A13EC6FE29B32BC6F255B208B7FACE43D7218A2685CC`；`git diff --check` 无空白错误。

结论：P1 已具备可玩的独立新战斗核心，后续战斗、灵根/天赋成长和新修行玩法可继续在 v2 规则集与 Application Service 上扩展，不需要修改冻结 HTML。长期成长、挂机大循环、CloudBase 和最终 UI/皮肤仍按文档留待后续阶段。

## 阶段 34：P2 成长闭环、联网骨架与洞府炼丹

状态：已完成本地代码与自动化验收；真实 CloudBase 环境部署和跨账号/跨设备联调待凭据与目标环境就绪后执行。

边界：严格依据 `凡修录_P2_成长闭环联网与洞府炼丹.md`；在 P1 `ruleset v2` 上补齐可发布的最小成长循环，不修改冻结单体，不改变 P0 恢复的 UI 风格、三栏骨架和旧交互，不恢复冻结系统，不增加第四功法槽、实时 PvP、公会或最终皮肤。

成长与奖励闭环：

- 建立独立 progression 领域，落地金、木、水、火、土五灵根，8 个轻量天赋、主副天赋组合、炼气十三层至元婴初期境界定义、结丹后期公开上限、固定成功突破和均衡/炼体/身法/神识/固守 5 种成长策略；旧等级和旧灵根显示值通过迁移层进入 v2，不污染 legacy 公式。
- 将战斗奖励扩展为修为、灵石、装备、功法、五种灵草、首领材料、图鉴和任务进度；背包满、气血不足、法力不足、死亡等停止原因由领域结果明确返回。
- 在线自动历练继续复用 P1 `BattleStateMachine`、三功法和固定 seed；15 分钟内离线逐场精确模拟，长离线采用固定样本聚合，收益最多结算 8 小时，并保存场次、胜负、资源、掉落、丹药消耗和停止原因摘要。

秘境、洞府与炼丹：

- 新增单人 20 层幽竹秘境，每 5 层首领，包含三种难度词缀、首领检查点、逐层首次奖励和自动深入；死亡只退回最近检查点，不恢复旧秘境系统。
- 洞府参悟使用单功法队列，支持 15/60/240 分钟，到期领取后提升初窥、熟练、精通、大成四级熟练度；熟练度经 v2 规则进入战斗，不由 Vue 或 Pinia 复制公式。
- 新建 `AlchemyV2`，使用五种灵草和四种基础丹方，固定成功率与产量，单队列最多 5 次；补充第 6 次入队拒绝回归，不读取旧炼丹等级、品质、炸炉、丹毒或自动炼丹。

CloudBase 联网骨架：

- 客户端仓储、云函数服务和内存模拟统一支持匿名身份入口、正式账号绑定、24 槽云存档、乐观并发、挂机结算、境界突破、公开快照、异步 PvP、三类排行榜和 AI 立绘任务状态。
- 数据模型拆分为 `users`、`character_saves`、`public_characters`、`pvp_snapshots`、`battle_records`、`appearance_jobs` 六集合；公开排行榜投影与 PvP 战斗快照分开保存，数据库对 Web 客户端默认拒绝直接读写。
- 云端校验 owner、版本、灵根/天赋不可变字段、境界突破条件、装备白名单和固定三功法；挂机、突破和 PvP 使用 `requestId` 实现幂等/重放保护，异步 PvP 由服务端固定 seed 调用 P1 状态机并保存 `BattleEvent`。
- 未配置环境时登录页和设置页明确显示本地降级，不阻断本地游戏。仓库不包含 CloudBase 环境 ID、凭据或 Web SDK 实例，因此真实部署、双账号、双设备、并发冲突和网络重试仍未标记完成。

旧 UI 原位接入与冻结范围：

- 保留暗绿/金色旧样式、桌面左角色栏—中标签页—右挂机栏、旧蓝天绿地战斗场景、角色/妖兽画面、六槽纸娃娃、储物袋格和装备并排比较；P2 仅在角色栏、挂机栏、区域、功法、储物袋和设置旧页面的对应位置增加必要控件。
- 修复移动端旧响应式规则隐藏整个装备侧栏时连带隐藏 P2 炼丹入口的问题：桌面结构和交互不变，移动端只暴露储物袋页内的炼丹折叠区，继续保持单列布局和无横向溢出。
- v2 不读取或推进悟道、转世、飞升、无尽节点、符纹、禁制、套装、队伍、同行、护道者、灵兽、旧秘境、旧炼丹、神龛、赌场、宗门和旧挂机经济；legacy 角色继续走原规则，二者公式与存档边界保持隔离。
- `legacy/fanxiulu-monolith.html` 未修改，冻结 SHA-256 继续为 `113F9ED589375E746C50A13EC6FE29B32BC6F255B208B7FACE43D7218A2685CC`。

交付与验证：

- 新增 `P2_IMPLEMENTATION_PLAN.md`、`P2_FEATURE_MATRIX.md`、`P2_FROZEN_FEATURES.md`、`P2_BALANCE_REPORT.md`、`P2_RELEASE_CHECKLIST.md` 和 `P2_KNOWN_DIFFERENCES.md`，同步更新项目与 CloudBase README。
- 冻结静态数据一致性、TypeScript `--noEmit`、项目引用构建、Vue 生产构建、CloudBase bundle 和冻结基线验收通过。
- 50 个单元/回归测试文件共 283 项全部通过，覆盖成长、奖励、挂机/离线、秘境、洞府、炼丹、云端权限、幂等、PvP、排行榜、legacy 隔离和 P0/P1 基线。
- 全量 Playwright 使用系统 Edge 验收：23 项通过、7 项按桌面/移动条件跳过、0 项失败；覆盖默认入口、冻结 legacy、旧 UI/装备交互、P1 回合制、P2 成长/挂机/秘境/洞府/炼丹，以及移动端核心二级页面和无横向溢出。

结论：P2 本地玩法闭环和可部署的联网代码已经完成，后续机制调整可以继续在 `src/game-core/rulesets/v2`、progression 领域和 Application Service 边界内迭代，不依赖修改冻结 HTML。真实 CloudBase 部署按用户安排延后，最终皮肤与交互设计继续留在玩法和删减范围定稿后的独立阶段。

## 阶段 35：P0-P2 游戏配置整理与人工验收审计

状态：配置与验收文档已完成；重新打开 3 项 P2 产品语义门禁，等待修复和人工复验。

结果：

- 新增 `P0_P2_MANUAL_ACCEPTANCE_GUIDE.md`，按当前实际运行时统一记录 P0 入口/存档/旧规则/旧 UI，P1 回合制/五行/三功法/内容白名单，以及 P2 灵根/天赋/境界/成长/掉落/挂机/离线/秘境/洞府/炼丹/CloudBase 配置。
- 为 P0、P1、P2 分别建立可逐项勾选的人工验收流程，并提供验收前备份要求、建议角色、入口地址、通过/有条件通过/不通过/待环境四种结论和最终记录表。
- 明确当前层次关系：P2 五灵根和成长领域已经接管新角色创建，P1 旧灵根档案仅保留为兼容数据；P2 战斗仍复用 P1 的身法先攻、五行和三功法状态机。
- 代码交叉审计发现历练目标只保存选择而未改变策略、29 件装备掉落池只有 12 件能通过奖励入包检查、护脉丹层数未被突破流程读取三处缺口。`P2_FEATURE_MATRIX.md` 已将对应项改为“需修复”，`P2_RELEASE_CHECKLIST.md` 已重新增加 3 个未勾选门禁，不再以现有自动化通过替代产品语义验收。
- README 已增加统一人工验收指南入口。真实 CloudBase 环境、账号绑定和跨设备/跨账号联调继续保持待环境状态。

兼容性：本阶段只修改维护文档，没有修改游戏规则、UI、交互、存档结构、CloudBase 代码或冻结 `legacy/fanxiulu-monolith.html`。

验证：交叉读取 P0/P1/P2 矩阵、冻结基线、实际规则源、Application Service、Pinia、页面控件和 E2E 用例；文档空白检查与冻结 SHA-256 校验通过。由于没有运行代码变动，不重复执行上一阶段已通过的 283 项测试和 30 项桌面/移动 E2E。

## 阶段 36：标准灵根体系、战斗可视状态与旧图标收尾

状态：本地规则、旧 UI 原位接入、自动化与浏览器验收已完成；真实 CloudBase 继续按产品安排延后。

边界：不修改 UI 风格、布局和交互，不修改冻结单体，不生成妖兽 SVG。战斗妖兽暂时使用旧版图标，等待产品后续提供 PNG；角色与角色库原有画面继续保留。

规则与存档：

- 将新 v2 角色的灵根体系统一为 31 种标准组合：五灵根 1 种、四灵根 5 种、三灵根 10 种、双灵根 10 种、天灵根 5 种。修炼和洞府倍率固定为 `1 / 灵根属性数量`，即 20% / 25% / 约 33% / 50% / 100%。
- 灵根只决定可学习的五行属性范围和修炼效率；实际使用的功法属性决定战斗克制。初始功法、已知功法、掉落、奖励、洞府参悟和云端存档校验统一执行该限制，无属性功法对所有灵根开放。
- 新 v2 角色只以 `rootId` 作为灵根真值。使用废弃灵根 ID 的旧 v2 角色槽不再兼容，载入时先生成本地备份再过滤；legacy 角色仍在独立旧规则集中运行。
- 手动战斗与自动历练统一在胜利后恢复最大气血 12% 和最大法力 16%，恢复值不超过上限；手动战斗日志记录实际恢复数值。

旧 UI 原位修复：

- 保留暗绿/金色三栏、旧蓝天绿地战斗场景、角色画面、妖兽旧图标、原战斗按钮、六槽装备和装备并排比较。妖兽节点只渲染旧图标，不包含 SVG。
- 恢复角色与妖兽同场显示、回合与当前行动者状态，以及攻击、受击、施法、治疗、入场和倒地动画；没有更换旧 UI 结构。
- `legacy-combat-log` 在每次增加消息后等待 DOM 更新并滚动到底部，长日志不会停留在旧位置。
- 离线收益改为离开后返回页面时弹窗；洞府和秘境的常规活动摘要使用独立类名，不再让 `legacy-offline-summary` 常驻主页面。
- 战利品日志通过中文显示名输出，不再直接暴露 `Rawhide Boots` 等内部 ID；游戏界面移除 P0/P1/P2 阶段代号。

文档与验收：

- 更新 `P0_P2_MANUAL_ACCEPTANCE_GUIDE.md`、P1/P2 功能矩阵、P2 数值报告、实施计划、发布清单、已知差异和 README，统一记录 31 种灵根、学习限制、废弃 v2 存档策略、战后恢复、离线弹窗和妖兽旧图标约束。
- 修正 P1/P2 E2E 创建流程对“天灵根”分组按钮的无障碍名称选择器，避免完整名称包含组合数量时精确匹配失败。

验证：

- 50 个单元/回归测试文件共 287 项全部通过；TypeScript `--noEmit`、项目引用构建、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性、冻结基线和 `git diff --check` 全部通过。
- 浏览器实测天灵根显示 100% 修炼速度，火天灵根只允许火属性和无属性功法；妖兽显示旧版蝙蝠图标且妖兽节点 SVG 数量为 0，攻击/施法动画类实际出现。
- 浏览器实测 24 条战斗日志后仍置底，最后一条为实际战后恢复；390×844 移动端无横向溢出、移动导航可见，控制台无错误，验收临时角色已删除并恢复测试视口。
- Playwright CLI 因本机未安装其单独下载的 Chromium 未完成本轮全量执行；用例选择器已修正，关键流程由应用内浏览器实测覆盖。冻结 `legacy/fanxiulu-monolith.html` 未修改。

## 阶段 37：First commit 自动历练可视战斗等价修复

状态：在线自动历练的妖兽显示、逐次行动、攻击/受击反馈和重新开启流程已修复，并完成规则测试与应用内浏览器动态验收。

问题确认与基线对照：

- GitHub 最早提交为 `d2ef6cd85bebae9ed32085536245129c4efaf7c1`。该提交中的单体页面与当前冻结 `legacy/fanxiulu-monolith.html` 使用同一 Git blob `f212cd0983babd4c713d346947012bb117ce02c3`，因此可以直接作为旧行为基线。
- 修复前的 v2 在线自动历练并不与 First commit 等价：每个定时 tick 调用一次纯规则整场模拟器，在同一同步调用中完成生成妖兽、双方全部行动、胜负和奖励结算。Vue 只收到战后角色结果，拿不到妖兽生成、出手、受击和倒地的中间状态，所以页面长期显示空场景。
- First commit 的旧 `afkTick` 每次只推进一个可见步骤：无妖兽时只生成妖兽并返回，有妖兽时只施放一次功法或普通攻击并返回。此次恢复的是这项在线交互行为等价，不把 v2 回合制规则、五行、灵根和成长系统回退成旧战斗公式。

修复结果：

- v2 在线自动历练改为复用 `combat.store` 和 `V2BattleApplication`：第一个 tick 准备补给并生成可见妖兽，后续每个 tick 只调用一次玩家自动策略命令；敌方回应仍由同一 `BattleStateMachine` 按身法顺序推进。
- 战斗结束后保留已完成妖兽状态 1.4 秒，用于完成伤害浮字和倒地动画，再清空场景；胜负、首领停止、背包满停止、奖励和战后恢复继续使用 v2 Application Service 的唯一结算链。
- 修复战败后再次开启自动历练会误读上一场失败结果并立即停止的问题：启动时记录已处理的完成战斗 ID，生成新妖兽时不再检查旧结算。
- 保留旧蓝天绿地场景、角色画面和妖兽 emoji 图标，不生成妖兽 SVG。恢复玩家/妖兽冲刺、施法、受击、治疗、入场、倒地以及普通/会心/治疗浮字；攻击动画类保留时间与 0.5 秒 CSS 动画对齐，不再提前截断。
- 战斗面板重新挂载时从当前日志末尾初始化动画游标，只播放新增 `BattleEvent`，不会把历史伤害和治疗浮字重新播放。`legacy-combat-log` 每次追加事件后继续自动置底。

验证：

- 新增在线自动历练逐次生成/行动回归，以及战败后重新开启不误读旧结果的回归；定向 24 项测试通过，最终全量 50 个测试文件共 289 项全部通过。
- 应用内浏览器使用独立临时角色实测：自动历练第一个行动间隔内出现妖兽，妖兽节点 SVG 数量为 0；攻击帧同时出现玩家 `is-attacking`、妖兽 `is-hit` 和伤害浮字 `-15`；战斗日志保持置底。
- 实景截图确认旧暗绿/金色三栏、蓝天绿地场景、角色与妖兽同屏、原战斗按钮和日志结构未改变。验收临时角色已删除，用户角色“栖月”及其槽位未修改。
- Playwright CLI 仍因本机缺少其单独下载的 Chromium 可执行文件而无法启动；相同关键流程已由应用内浏览器完成动态验收。冻结单体未修改。
- TypeScript 项目引用构建、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性、冻结基线和 `git diff --check` 全部通过。

结论：此前“在线自动历练与 First commit 可视行为等价”的判断不成立；本阶段已恢复逐次生成妖兽和逐次可见行动。当前实现与 First commit 在自动历练可见节奏和反馈目标上等价，但内部仍使用模块化 v2 状态机和新规则，不是把旧单体战斗源码重新接回运行时。

## 阶段 38：战后调息、治疗公式与行动锁闭环

状态：调息规则、自动历练衔接、治疗入口、状态展示和行动锁已完成，并通过自动化与应用内浏览器验收。

规则结果：

- v2 战斗不再提供“调息”命令，战斗中只有治疗功法和丹药可以恢复气血；两者都会消耗本回合行动，并正常进入敌方后续行动。
- 战斗结算后按角色当前 `v2AutoConfiguration.meditationThreshold` 判断；当 `气血 / 最大气血 <= 战后调息阈值` 时写入持久化 `v2ActionState` 并开始调息，包含恰好等于 50% 的边界。
- 点击开启自动历练时会先按当前气血和当前调息阈值执行同一判断；低血量且尚未进入战斗的角色会先调息，不会先寻敌、战败后才恢复。每次准备生成敌人前还会再次执行该检查，满血后才继续历练。
- 自然调息每秒恢复 `max(1, floor(体魄 / 3))` 点气血，体魄读取六维 `str`；恢复值不读取最大气血百分比。调息满血后清除行动状态，运行中的自动历练再继续寻敌。
- 回春丹固定按 `20 + 体魄 × 3` 恢复气血，不读取最大气血百分比；调息和战斗内共用同一公式并同步库存。
- 治疗功法改为逐门配置五行亲和和六维缩放。当前“青木回春”为木属性，恢复量按 `神识 × 1.2 + 根骨 × 0.35` 及其独立规则结算，不建立统一百分比治疗模板。
- 自动策略只在气血实际低于治疗阈值时使用治疗功法，只在法力低于回灵阈值时使用回灵功法，不再满气血空放治疗。

行动与界面：

- 调息期间禁止寻敌、区域切换、突破、成长策略、秘境、洞府参悟、炼丹、出售和调整功法；允许更换或卸下装备，并允许消耗回春丹或施展已拥有且法力足够的治疗功法加速恢复。
- 在线自动历练会在调息期间暂停寻敌，离线模拟会先结算调息耗时；调息结束后继续原自动历练流程。
- 修复阈值配置异步保存与开启自动历练同时发生时，较早保存结果覆盖最新调息状态的竞态；存档写入会检测期间的新状态并排队补写，不再用旧快照覆盖当前角色。
- 保持 P0 恢复的旧 UI 结构与风格，只在右侧原挂机栏显示“调息中”、当前气血、体魄、每秒恢复和预计剩余时间，并在旧斗法场景状态条同步显示“调息中”。相关寻敌和玩法按钮按行动锁禁用。
- 战报在触发时记录“战后气血达到或低于调息阈值”，避免 50% 等边界与“低于”文字产生歧义。

验证：

- 52 个单元/回归测试文件共 297 项全部通过，覆盖 50% 等值边界、在线自动历练暂停/恢复、战败后重启、自然调息、丹药/功法加速、行动锁与离线模拟。
- TypeScript 项目引用构建、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性和冻结基线全部通过；`legacy/fanxiulu-monolith.html` 未修改。
- 应用内浏览器使用独立临时角色实测：战后阈值设为 50%，角色以 48/100 气血结束战斗后立即进入调息；斗法场景与右侧栏同时显示“调息中”、每秒恢复 3 点和预计 18 秒，按钮锁定符合规则。另以自动历练完整流程确认战后写入 `RestStarted` 并暂停寻敌。

## 阶段 39：全局数值策划层与战胜恢复曲线

状态：统一配置模型、运行时接入、设置页配置台、逐怪掉落统一、规则测试、全量构建与浏览器验收均已完成。

配置架构：

- 新增 `src/game-core/rulesets/v2/balance.config.ts` 作为 v2 全局数值入口，集中暴露职业初始值、突破路线、职业成长倍率、12 门功法、五行克制、战斗默认值、29 件装备、22 只怪物、逐怪掉落和恢复/丹药参数。
- 新增 `src/stores/balance.store.ts`，配置使用本机键 `fanxiulu:v2-game-balance:v1` 持久化，不塞入单个角色存档；创建角色、突破、普通战斗、自动历练、离线、秘境、装备和恢复读取同一运行时配置。
- 战斗状态只保存战斗、五行和丹药所需的小型配置快照，不深拷贝完整怪物、装备和功法目录。
- 修改装备基础属性时，现有 v2 角色已穿戴装备按新旧差值重算最大资源、攻击、防御和六维；职业初始值只影响后续新角色，职业成长值只影响后续突破。

战斗与恢复：

- 战胜恢复替换阶段 36 的固定 12% 气血和 16% 法力。新公式为 `10% 最大资源 + 经过软上限衰减的 属性 × 修为等级 × 0.10`，成长软上限为最大资源 25%，单次硬上限为最大资源 35%。
- 气血读取体魄 `str`，法力读取神识 `wis`；装备结算后的最大资源和六维自然参与公式。只有胜利恢复，战败保留 1 点气血，逃离不恢复。
- 统一结算顺序为“胜利奖励 -> 战胜恢复 -> 按恢复后的气血判断调息阈值”；战报记录实际气血和法力恢复量及基础/成长分项。
- 将基础致命概率、身法致命系数、致命倍率、命中、闪避、防御曲线、伤害浮动和遁走概率从匿名常数迁入配置。默认命中 100%、闪避 0%，因此未主动调参时不改变既有必中特性。

掉落与界面：

- 手动、自动、离线精确模拟和秘境统一使用逐怪掉落配置；每只怪可独立设置绑定装备及概率、随机装备池、灵草池、功法池、各项掉率和材料数量。
- 设置旧页面新增原位“数值配置台”，沿用暗绿/金色样式，不修改旧三栏、战斗场景、装备交互和页面结构。配置台按战斗与五行、职业成长、功法、装备、怪物与掉落、恢复与丹药、JSON 七组编辑。
- 配置台支持当前角色战胜恢复预览、应用全部参数、恢复推荐值、JSON 校验导入、复制和下载；战斗、自动历练或调息期间禁止应用。

文档与测试：

- 新增 `V2_GAME_BALANCE_CONFIGURATION.md`，记录资源池审计、公式推导、阶段样例、所有配置分组、生效时机和调参建议。
- 新增全局配置与恢复曲线规则测试，并更新旧的“战后不恢复”断言为“先恢复、再判断调息”。冻结 `legacy/fanxiulu-monolith.html` 未修改。
- 修复旧规则角色误显示 v2 战胜恢复预览的问题；只有 v2 角色才展示依赖体魄、神识和修为等级的预估结果。
- 53 个单元/回归测试文件共 304 项全部通过；TypeScript `--noEmit`、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性、冻结基线和 `git diff --check` 全部通过。
- 应用内浏览器实测配置台七组均可访问：装备下拉为 29 项，怪物下拉为 22 项并包含三个独立掉落池，恢复页为 13 项参数，JSON 页包含完整编辑区与四项操作；全程未应用或重置用户数值配置。
- 390×844 移动端实测配置台字段无越界；同时修复旧底部导航固定按钮宽度与隐藏存档输入受全局宽度规则覆盖造成的横向滚动，页面、导航和配置正文均满足 `scrollWidth === clientWidth`，没有改变原 UI 结构或交互。

## 阶段 40：战败续练、物品图标、对比箭头与全量内容开放

状态：自动历练战败后不再置停、储物袋图标与红绿箭头恢复、全部怪物/装备/历练区域开放，自动化与构建验收已完成。

自动历练与离线：

- 在线自动历练的战败分支不再写入 `角色死亡` 停止原因；战败结算仍保留 1 点气血并写入 `RestStarted` 调息状态，满血后由原自动历练定时器继续寻敌。
- 离线精确模拟遇到战败后进入下一轮调息并继续战斗；长离线聚合遇到战败只累加场次并继续，不再截断收益。
- 保留“首领战停止”和“背包已满停止”两个原有可选停止条件；新增回归覆盖战败后 `stopReason === null` 且角色进入调息，以及离线摘要 `stopReason === '离线时间耗尽'`。

储物袋与装备对比：

- 按冻结旧版 `getItemTileIcon` 原逻辑恢复物品图标：符纹 `🔣`、容器 `🎒`、武器按法杖/弓/锤/斧/枪/匕首细分，衣袍、护腿、鞋履、副手和饰品使用旧图标。
- 储物袋物品格恢复 `▲/▼/→` 强弱标记：与当前槽位装备按攻击、防御、气血、法力最大差值比较，绿色 `▲`、红色 `▼`、灰色 `→`，交互和旧详情弹窗保持不变。
- 物品详情对比中的攻击/防御/气血/法力差值与五行抗性差值同步恢复红绿箭头，替代原先只有文字颜色的显示。

内容全量开放：

- `V2_ENABLED_ZONE_INDEXES` 从 5 个扩展为旧版全部 41 个历练区域；`V2_ENABLED_EQUIPMENT_IDS` 从 29 件扩展为旧版全部带部位装备。
- `src/game-core/rulesets/v2/content.ts` 改为从冻结静态数据生成完整目录：41 个区域、123 只普通妖兽、82 只命名/精英、19 只首领；前五区旧 ID 与绑定掉落保留，避免破坏现有存档和既有测试。
- 自动寻敌从每个区域的 `eliteIds` 中随机选取精英；配置归一化会把旧存档中每个怪物的装备/灵草/功法池与新默认池做并集，因此旧配置无需重置即可看到新装备与新怪物。
- 全量怪物的等级、气血、攻击、防御、神识、体魄、身法、抗性、修为和灵石按旧版区域倍率与首领倍率生成，仍可在数值配置台中逐只调整。

验证：

- 53 个单元/回归测试文件共 306 项全部通过；TypeScript `--noEmit`、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性、冻结基线和 `git diff --check` 全部通过。
- 新增测试断言区域 41、普通 123、精英 82、首领 19、装备覆盖全部旧版目录；战败续练、离线续练和首领区域开放均纳入回归。
- `legacy/fanxiulu-monolith.html` 未修改。

## 阶段 41：场景灵力充沛度与被动法力恢复

状态：41 个区域的灵力充沛度、被动法力恢复公式、配置台分组和调息行为修正已完成，自动化与构建验收通过。

规则结果：

- 调息不再恢复法力；调息只按体魄恢复气血。法力改为按时间被动恢复，不受调息状态限制。
- 被动法力恢复公式为 `max(保底值, 基础值 + 神识 × 灵力充沛度 × 神识系数) × (1 + 境界序号 × 境界系数)`。
- 神识读取六维 `wis`；境界序号按炼气一层起递增，炼气一层为 0，每提升一档按境界系数成长。
- 默认参数：基础 0.5/秒、每点神识 0.15、境界成长 0.08、保底 0.25/秒。
- 41 个区域均有独立灵力充沛度，默认按区域等级从约 1.0 递增到约 3.4；回灵丹与回灵功法仍作为主动加速手段。

配置与界面：

- `V2ZoneDefinition` 新增 `spiritualAbundance`，数值配置台新增“场景灵力”分组，可逐区域调整充沛度；恢复与丹药页新增四个法力恢复参数。
- 角色状态栏新增“法力恢复/秒”和“灵力充沛”显示；历练区域页在 v2 区域收益中显示当前灵力充沛度。
- 在线定时器每秒同时推进气血调息与被动法力恢复；离线精确/聚合模拟按战斗、调息和剩余时间累计法力恢复。
- 旧配置无需重置：`zoneAbundance` 会按默认全量区域合并，法力恢复参数使用默认值进入配置 JSON。

验证：

- 新增测试覆盖神识/区域充沛度/境界对恢复速度的影响、按时间累计与法力上限约束、调息不恢复法力；恢复与配置测试、离线续练测试均通过。
- 53 个单元/回归测试文件共 309 项全部通过；TypeScript `--noEmit`、Vue 生产构建、CloudBase 隔离 bundle、冻结静态数据一致性、冻结基线和 `git diff --check` 全部通过。
- `legacy/fanxiulu-monolith.html` 未修改。
