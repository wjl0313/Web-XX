# P2 已知差异

## 当前未通过的人工验收门禁

- 自动历练的冲击境界、积攒灵石、寻找功法、寻找灵草四个目标当前只保存并显示选择，尚未改变寻敌或掉落策略。
- 护脉丹服用后会记录护脉层数，但境界突破当前没有读取或消耗该层数。

这两项属于待修复缺口，不是允许长期保留的设计差异。原 29 件装备入包缺口已修复并转入人工复验。人工验收步骤见 `P0_P2_MANUAL_ACCEPTANCE_GUIDE.md`。

## 真实云环境未部署

仓库已包含客户端仓储、服务端函数、六集合、索引和安全规则，但不包含 CloudBase 环境 ID、登录凭据或 Web SDK 实例。当前本地构建会显示“当前构建未配置云环境”。真实跨设备同步、账号提供商绑定和跨账号挑战必须在目标环境部署后联调。

## 本地与云端模式

- 本地入口继续完整可玩；本地突破和本地离线收益不依赖网络。
- 云角色的权威突破、离线领取、公开快照和斗法通过云函数执行。
- 普通云保存允许同步本地 PvE 成长，但禁止改写灵根、天赋和境界；多人竞争数据仍以云函数为准。

## P2 内容量

P2 继续使用 P1 的 4 个传承、5 个区域、12 门功法和 29 件装备白名单，并新增一个 20 层秘境、5 种灵草和 4 种丹药。旧仓库中的大批区域、妖兽、功法和装备不会自动进入 v2。

## 自动历练与离线

- 在线自动历练逐场精确模拟。
- 15 分钟以内离线逐场模拟；更长时间使用 12 场样本估算并聚合，结果可复现但不等同于逐帧回放全部战斗。
- 离线摘要的 `elapsedMs` 记录实际离线时长，`simulatedMs` 记录被 8 小时上限截断后的收益模拟时长。

## UI

P2 没有进行最终换皮或交互重做。新增信息沿用旧面板、按钮、表格和左右栏位置。妖兽战斗形象暂用旧版图标，不新增妖兽 SVG；产品后续提供 PNG 后再进行素材替换。最终皮肤仍应在玩法与删减范围定稿后独立设计。

## 冻结数据

旧悟道、转世、飞升、符纹、队伍、灵兽、护道者、旧秘境和旧炼丹字段仍可随旧存档保存，但不会影响 v2 战斗、掉落、挂机、秘境、洞府或炼丹。
