# P0 已知差异报告

本文记录 `/?mode=equivalent` 与冻结旧版 `/?mode=legacy` 在 P0 收口后的有意差异。核心玩家流程不存在待修复的功能阻断差异。

## 1. 有意冻结的功能差异

- 赌场、聊天、宗门、Discord、管理员和 LFG 不进入原生主导航，也不会注册轮询、实时订阅、计时器或奖励结算。
- 悟道、重修/飞升、符纹、禁制套装、护道者、同行、灵兽、旧炼丹、神龛和套装收藏仅在“旧版兼容工具”读取存档摘要；原生主流程不允许推进或消耗资源。
- 云端道籍与 CloudBase 权威结算不在 P0 接入；原生登录页保留旧式布局，但只开放本地进入。
- `/?mode=legacy` 仍可查看冻结旧版的完整外围页面；原生入口不会删除对应旧存档字段。

## 2. 非阻断呈现差异

- 原生壳使用 Vue 组件、语义化元素和 CSS 重新实现旧界面，没有复制旧版 DOM 或运行时脚本。
- 角色和战斗形象继续读取旧外观字段，但原生 SVG 的细节与冻结脚本生成图形并非像素级一致。
- 冻结旧版顶部“更多”和部分外围入口在原生壳中隐藏或禁用，以符合 P0 冻结要求。
- 移动端使用固定底部核心导航；桌面端继续使用顶部标签栏和左角色栏—中内容区—右挂机栏结构。

以上差异不得改变旧规则、旧数值、存档字段、六槽装备、装备比较或核心操作路径。

## 3. UI/交互冻结基准

- 基准提交：`07919d35ba192e28570c9bf4a19a52da5dd86ff9`。
- 基准文件：`legacy/fanxiulu-monolith.html`。
- P0 不允许统一换皮、改成纯文字战斗、替换装备比较交互或重新设计角色库结构。
- 后续若需要修改皮肤或交互，必须作为独立阶段由产品统一设计，不得混入 P0/P1 规则开发。

## 4. 阻断判定

以下任一情况重新出现，都应视为 P0 回归缺陷：

- 默认入口重新依赖 iframe；
- 角色库、创建、区域、战斗、六槽装备、背包比较、功法、挂机、离线或存档流程不可用；
- 冻结功能在后台推进或消耗资源；
- 旧字段丢失，或冻结 HTML 哈希变化；
- 玩家界面出现明显英文残留；
- 原生界面再次偏离冻结旧版的风格和核心交互结构。

