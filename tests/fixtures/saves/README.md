# 存档测试夹具

这里只允许提交脱敏或人工构造的存档，不得提交真实账号、令牌、聊天内容或玩家个人信息。

`empty-slots-v1.json` 表示旧版 `EmberQuest_slots` 的空角色槽状态，用于登录和角色道册主路径测试。

`legacy-character-v2.json` 是人工构造的 schema 2 单角色存档，用于验证 24 槽结构、旧字段读取和健康戳，不包含真实玩家数据。
